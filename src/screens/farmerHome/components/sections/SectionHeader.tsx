// Moved to components/molecules/SectionHeader.tsx (merged with the
// byte-identical copy at screens/hub/components/community/SectionHeader.tsx).
// Re-exported here so existing call sites in this folder don't need to change.
export { SectionHeader } from '../../../../components/molecules/SectionHeader';
