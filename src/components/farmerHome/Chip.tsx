// Moved to components/atoms/Chip.tsx (promoted so folders outside
// farmerHome/ can import it without reaching into a feature folder).
// Re-exported here so the one existing call site doesn't need to change.
export { Chip } from '../atoms/Chip';

